import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const PaymentListScreen = (props: Props) => {
  return (
    <View>
      <Text>PaymentListScreen</Text>
    </View>
  );
};

export default PaymentListScreen;

const styles = StyleSheet.create({});
