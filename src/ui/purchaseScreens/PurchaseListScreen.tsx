import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const PurchaseListScreen = (props: Props) => {
  return (
    <View>
      <Text>PurchaseListScreen</Text>
    </View>
  );
};

export default PurchaseListScreen;

const styles = StyleSheet.create({});
